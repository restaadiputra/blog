import styled from 'styled-components';
import tw from 'tailwind.macro';

export const Banner = styled.section`
  ${tw`bg-white border-b border-gray-100 shadow-md`};
`;

export const Content = styled.p`
  ${tw`mb-8`};
`;
